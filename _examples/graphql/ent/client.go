// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"log"
	"reflect"

	"_examples/graphql/ent/migrate"

	"_examples/graphql/ent/testexclude"
	"_examples/graphql/ent/testskip"
	"_examples/graphql/ent/testskiphistory"
	"_examples/graphql/ent/todo"
	"_examples/graphql/ent/todohistory"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"

	"github.com/flume/enthistory"
)

// Client is the client that holds all ent builders.
type Client struct {
	config
	// Schema is the client for creating, migrating and dropping schema.
	Schema *migrate.Schema
	// TestExclude is the client for interacting with the TestExclude builders.
	TestExclude *TestExcludeClient
	// TestSkip is the client for interacting with the TestSkip builders.
	TestSkip *TestSkipClient
	// TestSkipHistory is the client for interacting with the TestSkipHistory builders.
	TestSkipHistory *TestSkipHistoryClient
	// Todo is the client for interacting with the Todo builders.
	Todo *TodoClient
	// TodoHistory is the client for interacting with the TodoHistory builders.
	TodoHistory *TodoHistoryClient

	// historyActivated determines if the history hooks have already been activated
	historyActivated bool
}

// NewClient creates a new client configured with the given options.
func NewClient(opts ...Option) *Client {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	client := &Client{config: cfg}
	client.init()
	return client
}

func (c *Client) init() {
	c.Schema = migrate.NewSchema(c.driver)
	c.TestExclude = NewTestExcludeClient(c.config)
	c.TestSkip = NewTestSkipClient(c.config)
	c.TestSkipHistory = NewTestSkipHistoryClient(c.config)
	c.Todo = NewTodoClient(c.config)
	c.TodoHistory = NewTodoHistoryClient(c.config)
}

// withHistory adds the history hooks to the appropriate schemas - generated by enthistory
func (c *Client) WithHistory() {
	if !c.historyActivated {
		for _, hook := range enthistory.HistoryHooks[*TestSkipMutation]() {
			c.TestSkip.Use(hook)
		}
		for _, hook := range enthistory.HistoryHooks[*TodoMutation]() {
			c.Todo.Use(hook)
		}
		c.historyActivated = true
	}
}

type (
	// config is the configuration for the client and its builder.
	config struct {
		// driver used for executing database requests.
		driver dialect.Driver
		// debug enable a debug logging.
		debug bool
		// log used for logging on debug mode.
		log func(...any)
		// hooks to execute on mutations.
		hooks *hooks
		// interceptors to execute on queries.
		inters *inters
	}
	// Option function to configure the client.
	Option func(*config)
)

// newConfig creates a new config for the client.
func newConfig(opts ...Option) config {
	cfg := config{log: log.Println, hooks: &hooks{}, inters: &inters{}}
	cfg.options(opts...)
	return cfg
}

// options applies the options on the config object.
func (c *config) options(opts ...Option) {
	for _, opt := range opts {
		opt(c)
	}
	if c.debug {
		c.driver = dialect.Debug(c.driver, c.log)
	}
}

// Debug enables debug logging on the ent.Driver.
func Debug() Option {
	return func(c *config) {
		c.debug = true
	}
}

// Log sets the logging function for debug mode.
func Log(fn func(...any)) Option {
	return func(c *config) {
		c.log = fn
	}
}

// Driver configures the client driver.
func Driver(driver dialect.Driver) Option {
	return func(c *config) {
		c.driver = driver
	}
}

// Open opens a database/sql.DB specified by the driver name and
// the data source name, and returns a new client attached to it.
// Optional parameters can be added for configuring the client.
func Open(driverName, dataSourceName string, options ...Option) (*Client, error) {
	switch driverName {
	case dialect.MySQL, dialect.Postgres, dialect.SQLite:
		drv, err := sql.Open(driverName, dataSourceName)
		if err != nil {
			return nil, err
		}
		return NewClient(append(options, Driver(drv))...), nil
	default:
		return nil, fmt.Errorf("unsupported driver: %q", driverName)
	}
}

// ErrTxStarted is returned when trying to start a new transaction from a transactional client.
var ErrTxStarted = errors.New("ent: cannot start a transaction within a transaction")

// Tx returns a new transactional client. The provided context
// is used until the transaction is committed or rolled back.
func (c *Client) Tx(ctx context.Context) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, ErrTxStarted
	}
	tx, err := newTx(ctx, c.driver)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = tx
	return &Tx{
		ctx:             ctx,
		config:          cfg,
		TestExclude:     NewTestExcludeClient(cfg),
		TestSkip:        NewTestSkipClient(cfg),
		TestSkipHistory: NewTestSkipHistoryClient(cfg),
		Todo:            NewTodoClient(cfg),
		TodoHistory:     NewTodoHistoryClient(cfg),
	}, nil
}

// BeginTx returns a transactional client with specified options.
func (c *Client) BeginTx(ctx context.Context, opts *sql.TxOptions) (*Tx, error) {
	if _, ok := c.driver.(*txDriver); ok {
		return nil, errors.New("ent: cannot start a transaction within a transaction")
	}
	tx, err := c.driver.(interface {
		BeginTx(context.Context, *sql.TxOptions) (dialect.Tx, error)
	}).BeginTx(ctx, opts)
	if err != nil {
		return nil, fmt.Errorf("ent: starting a transaction: %w", err)
	}
	cfg := c.config
	cfg.driver = &txDriver{tx: tx, drv: c.driver}
	return &Tx{
		ctx:             ctx,
		config:          cfg,
		TestExclude:     NewTestExcludeClient(cfg),
		TestSkip:        NewTestSkipClient(cfg),
		TestSkipHistory: NewTestSkipHistoryClient(cfg),
		Todo:            NewTodoClient(cfg),
		TodoHistory:     NewTodoHistoryClient(cfg),
	}, nil
}

// Debug returns a new debug-client. It's used to get verbose logging on specific operations.
//
//	client.Debug().
//		TestExclude.
//		Query().
//		Count(ctx)
func (c *Client) Debug() *Client {
	if c.debug {
		return c
	}
	cfg := c.config
	cfg.driver = dialect.Debug(c.driver, c.log)
	client := &Client{config: cfg}
	client.init()
	return client
}

// Close closes the database connection and prevents new queries from starting.
func (c *Client) Close() error {
	return c.driver.Close()
}

// Use adds the mutation hooks to all the entity clients.
// In order to add hooks to a specific client, call: `client.Node.Use(...)`.
func (c *Client) Use(hooks ...Hook) {
	c.TestExclude.Use(hooks...)
	c.TestSkip.Use(hooks...)
	c.TestSkipHistory.Use(hooks...)
	c.Todo.Use(hooks...)
	c.TodoHistory.Use(hooks...)
}

// Intercept adds the query interceptors to all the entity clients.
// In order to add interceptors to a specific client, call: `client.Node.Intercept(...)`.
func (c *Client) Intercept(interceptors ...Interceptor) {
	c.TestExclude.Intercept(interceptors...)
	c.TestSkip.Intercept(interceptors...)
	c.TestSkipHistory.Intercept(interceptors...)
	c.Todo.Intercept(interceptors...)
	c.TodoHistory.Intercept(interceptors...)
}

// Mutate implements the ent.Mutator interface.
func (c *Client) Mutate(ctx context.Context, m Mutation) (Value, error) {
	switch m := m.(type) {
	case *TestExcludeMutation:
		return c.TestExclude.mutate(ctx, m)
	case *TestSkipMutation:
		return c.TestSkip.mutate(ctx, m)
	case *TestSkipHistoryMutation:
		return c.TestSkipHistory.mutate(ctx, m)
	case *TodoMutation:
		return c.Todo.mutate(ctx, m)
	case *TodoHistoryMutation:
		return c.TodoHistory.mutate(ctx, m)
	default:
		return nil, fmt.Errorf("ent: unknown mutation type %T", m)
	}
}

// TestExcludeClient is a client for the TestExclude schema.
type TestExcludeClient struct {
	config
}

// NewTestExcludeClient returns a client for the TestExclude from the given config.
func NewTestExcludeClient(c config) *TestExcludeClient {
	return &TestExcludeClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `testexclude.Hooks(f(g(h())))`.
func (c *TestExcludeClient) Use(hooks ...Hook) {
	c.hooks.TestExclude = append(c.hooks.TestExclude, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `testexclude.Intercept(f(g(h())))`.
func (c *TestExcludeClient) Intercept(interceptors ...Interceptor) {
	c.inters.TestExclude = append(c.inters.TestExclude, interceptors...)
}

// Create returns a builder for creating a TestExclude entity.
func (c *TestExcludeClient) Create() *TestExcludeCreate {
	mutation := newTestExcludeMutation(c.config, OpCreate)
	return &TestExcludeCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of TestExclude entities.
func (c *TestExcludeClient) CreateBulk(builders ...*TestExcludeCreate) *TestExcludeCreateBulk {
	return &TestExcludeCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *TestExcludeClient) MapCreateBulk(slice any, setFunc func(*TestExcludeCreate, int)) *TestExcludeCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &TestExcludeCreateBulk{err: fmt.Errorf("calling to TestExcludeClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*TestExcludeCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &TestExcludeCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for TestExclude.
func (c *TestExcludeClient) Update() *TestExcludeUpdate {
	mutation := newTestExcludeMutation(c.config, OpUpdate)
	return &TestExcludeUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *TestExcludeClient) UpdateOne(te *TestExclude) *TestExcludeUpdateOne {
	mutation := newTestExcludeMutation(c.config, OpUpdateOne, withTestExclude(te))
	return &TestExcludeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *TestExcludeClient) UpdateOneID(id uuid.UUID) *TestExcludeUpdateOne {
	mutation := newTestExcludeMutation(c.config, OpUpdateOne, withTestExcludeID(id))
	return &TestExcludeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for TestExclude.
func (c *TestExcludeClient) Delete() *TestExcludeDelete {
	mutation := newTestExcludeMutation(c.config, OpDelete)
	return &TestExcludeDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *TestExcludeClient) DeleteOne(te *TestExclude) *TestExcludeDeleteOne {
	return c.DeleteOneID(te.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *TestExcludeClient) DeleteOneID(id uuid.UUID) *TestExcludeDeleteOne {
	builder := c.Delete().Where(testexclude.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &TestExcludeDeleteOne{builder}
}

// Query returns a query builder for TestExclude.
func (c *TestExcludeClient) Query() *TestExcludeQuery {
	return &TestExcludeQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeTestExclude},
		inters: c.Interceptors(),
	}
}

// Get returns a TestExclude entity by its id.
func (c *TestExcludeClient) Get(ctx context.Context, id uuid.UUID) (*TestExclude, error) {
	return c.Query().Where(testexclude.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *TestExcludeClient) GetX(ctx context.Context, id uuid.UUID) *TestExclude {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *TestExcludeClient) Hooks() []Hook {
	return c.hooks.TestExclude
}

// Interceptors returns the client interceptors.
func (c *TestExcludeClient) Interceptors() []Interceptor {
	return c.inters.TestExclude
}

func (c *TestExcludeClient) mutate(ctx context.Context, m *TestExcludeMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&TestExcludeCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&TestExcludeUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&TestExcludeUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&TestExcludeDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown TestExclude mutation op: %q", m.Op())
	}
}

// TestSkipClient is a client for the TestSkip schema.
type TestSkipClient struct {
	config
}

// NewTestSkipClient returns a client for the TestSkip from the given config.
func NewTestSkipClient(c config) *TestSkipClient {
	return &TestSkipClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `testskip.Hooks(f(g(h())))`.
func (c *TestSkipClient) Use(hooks ...Hook) {
	c.hooks.TestSkip = append(c.hooks.TestSkip, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `testskip.Intercept(f(g(h())))`.
func (c *TestSkipClient) Intercept(interceptors ...Interceptor) {
	c.inters.TestSkip = append(c.inters.TestSkip, interceptors...)
}

// Create returns a builder for creating a TestSkip entity.
func (c *TestSkipClient) Create() *TestSkipCreate {
	mutation := newTestSkipMutation(c.config, OpCreate)
	return &TestSkipCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of TestSkip entities.
func (c *TestSkipClient) CreateBulk(builders ...*TestSkipCreate) *TestSkipCreateBulk {
	return &TestSkipCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *TestSkipClient) MapCreateBulk(slice any, setFunc func(*TestSkipCreate, int)) *TestSkipCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &TestSkipCreateBulk{err: fmt.Errorf("calling to TestSkipClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*TestSkipCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &TestSkipCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for TestSkip.
func (c *TestSkipClient) Update() *TestSkipUpdate {
	mutation := newTestSkipMutation(c.config, OpUpdate)
	return &TestSkipUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *TestSkipClient) UpdateOne(ts *TestSkip) *TestSkipUpdateOne {
	mutation := newTestSkipMutation(c.config, OpUpdateOne, withTestSkip(ts))
	return &TestSkipUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *TestSkipClient) UpdateOneID(id uuid.UUID) *TestSkipUpdateOne {
	mutation := newTestSkipMutation(c.config, OpUpdateOne, withTestSkipID(id))
	return &TestSkipUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for TestSkip.
func (c *TestSkipClient) Delete() *TestSkipDelete {
	mutation := newTestSkipMutation(c.config, OpDelete)
	return &TestSkipDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *TestSkipClient) DeleteOne(ts *TestSkip) *TestSkipDeleteOne {
	return c.DeleteOneID(ts.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *TestSkipClient) DeleteOneID(id uuid.UUID) *TestSkipDeleteOne {
	builder := c.Delete().Where(testskip.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &TestSkipDeleteOne{builder}
}

// Query returns a query builder for TestSkip.
func (c *TestSkipClient) Query() *TestSkipQuery {
	return &TestSkipQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeTestSkip},
		inters: c.Interceptors(),
	}
}

// Get returns a TestSkip entity by its id.
func (c *TestSkipClient) Get(ctx context.Context, id uuid.UUID) (*TestSkip, error) {
	return c.Query().Where(testskip.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *TestSkipClient) GetX(ctx context.Context, id uuid.UUID) *TestSkip {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *TestSkipClient) Hooks() []Hook {
	return c.hooks.TestSkip
}

// Interceptors returns the client interceptors.
func (c *TestSkipClient) Interceptors() []Interceptor {
	return c.inters.TestSkip
}

func (c *TestSkipClient) mutate(ctx context.Context, m *TestSkipMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&TestSkipCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&TestSkipUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&TestSkipUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&TestSkipDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown TestSkip mutation op: %q", m.Op())
	}
}

// TestSkipHistoryClient is a client for the TestSkipHistory schema.
type TestSkipHistoryClient struct {
	config
}

// NewTestSkipHistoryClient returns a client for the TestSkipHistory from the given config.
func NewTestSkipHistoryClient(c config) *TestSkipHistoryClient {
	return &TestSkipHistoryClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `testskiphistory.Hooks(f(g(h())))`.
func (c *TestSkipHistoryClient) Use(hooks ...Hook) {
	c.hooks.TestSkipHistory = append(c.hooks.TestSkipHistory, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `testskiphistory.Intercept(f(g(h())))`.
func (c *TestSkipHistoryClient) Intercept(interceptors ...Interceptor) {
	c.inters.TestSkipHistory = append(c.inters.TestSkipHistory, interceptors...)
}

// Create returns a builder for creating a TestSkipHistory entity.
func (c *TestSkipHistoryClient) Create() *TestSkipHistoryCreate {
	mutation := newTestSkipHistoryMutation(c.config, OpCreate)
	return &TestSkipHistoryCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of TestSkipHistory entities.
func (c *TestSkipHistoryClient) CreateBulk(builders ...*TestSkipHistoryCreate) *TestSkipHistoryCreateBulk {
	return &TestSkipHistoryCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *TestSkipHistoryClient) MapCreateBulk(slice any, setFunc func(*TestSkipHistoryCreate, int)) *TestSkipHistoryCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &TestSkipHistoryCreateBulk{err: fmt.Errorf("calling to TestSkipHistoryClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*TestSkipHistoryCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &TestSkipHistoryCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for TestSkipHistory.
func (c *TestSkipHistoryClient) Update() *TestSkipHistoryUpdate {
	mutation := newTestSkipHistoryMutation(c.config, OpUpdate)
	return &TestSkipHistoryUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *TestSkipHistoryClient) UpdateOne(tsh *TestSkipHistory) *TestSkipHistoryUpdateOne {
	mutation := newTestSkipHistoryMutation(c.config, OpUpdateOne, withTestSkipHistory(tsh))
	return &TestSkipHistoryUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *TestSkipHistoryClient) UpdateOneID(id uuid.UUID) *TestSkipHistoryUpdateOne {
	mutation := newTestSkipHistoryMutation(c.config, OpUpdateOne, withTestSkipHistoryID(id))
	return &TestSkipHistoryUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for TestSkipHistory.
func (c *TestSkipHistoryClient) Delete() *TestSkipHistoryDelete {
	mutation := newTestSkipHistoryMutation(c.config, OpDelete)
	return &TestSkipHistoryDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *TestSkipHistoryClient) DeleteOne(tsh *TestSkipHistory) *TestSkipHistoryDeleteOne {
	return c.DeleteOneID(tsh.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *TestSkipHistoryClient) DeleteOneID(id uuid.UUID) *TestSkipHistoryDeleteOne {
	builder := c.Delete().Where(testskiphistory.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &TestSkipHistoryDeleteOne{builder}
}

// Query returns a query builder for TestSkipHistory.
func (c *TestSkipHistoryClient) Query() *TestSkipHistoryQuery {
	return &TestSkipHistoryQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeTestSkipHistory},
		inters: c.Interceptors(),
	}
}

// Get returns a TestSkipHistory entity by its id.
func (c *TestSkipHistoryClient) Get(ctx context.Context, id uuid.UUID) (*TestSkipHistory, error) {
	return c.Query().Where(testskiphistory.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *TestSkipHistoryClient) GetX(ctx context.Context, id uuid.UUID) *TestSkipHistory {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *TestSkipHistoryClient) Hooks() []Hook {
	return c.hooks.TestSkipHistory
}

// Interceptors returns the client interceptors.
func (c *TestSkipHistoryClient) Interceptors() []Interceptor {
	return c.inters.TestSkipHistory
}

func (c *TestSkipHistoryClient) mutate(ctx context.Context, m *TestSkipHistoryMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&TestSkipHistoryCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&TestSkipHistoryUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&TestSkipHistoryUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&TestSkipHistoryDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown TestSkipHistory mutation op: %q", m.Op())
	}
}

// TodoClient is a client for the Todo schema.
type TodoClient struct {
	config
}

// NewTodoClient returns a client for the Todo from the given config.
func NewTodoClient(c config) *TodoClient {
	return &TodoClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `todo.Hooks(f(g(h())))`.
func (c *TodoClient) Use(hooks ...Hook) {
	c.hooks.Todo = append(c.hooks.Todo, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `todo.Intercept(f(g(h())))`.
func (c *TodoClient) Intercept(interceptors ...Interceptor) {
	c.inters.Todo = append(c.inters.Todo, interceptors...)
}

// Create returns a builder for creating a Todo entity.
func (c *TodoClient) Create() *TodoCreate {
	mutation := newTodoMutation(c.config, OpCreate)
	return &TodoCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of Todo entities.
func (c *TodoClient) CreateBulk(builders ...*TodoCreate) *TodoCreateBulk {
	return &TodoCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *TodoClient) MapCreateBulk(slice any, setFunc func(*TodoCreate, int)) *TodoCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &TodoCreateBulk{err: fmt.Errorf("calling to TodoClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*TodoCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &TodoCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for Todo.
func (c *TodoClient) Update() *TodoUpdate {
	mutation := newTodoMutation(c.config, OpUpdate)
	return &TodoUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *TodoClient) UpdateOne(t *Todo) *TodoUpdateOne {
	mutation := newTodoMutation(c.config, OpUpdateOne, withTodo(t))
	return &TodoUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *TodoClient) UpdateOneID(id uuid.UUID) *TodoUpdateOne {
	mutation := newTodoMutation(c.config, OpUpdateOne, withTodoID(id))
	return &TodoUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for Todo.
func (c *TodoClient) Delete() *TodoDelete {
	mutation := newTodoMutation(c.config, OpDelete)
	return &TodoDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *TodoClient) DeleteOne(t *Todo) *TodoDeleteOne {
	return c.DeleteOneID(t.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *TodoClient) DeleteOneID(id uuid.UUID) *TodoDeleteOne {
	builder := c.Delete().Where(todo.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &TodoDeleteOne{builder}
}

// Query returns a query builder for Todo.
func (c *TodoClient) Query() *TodoQuery {
	return &TodoQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeTodo},
		inters: c.Interceptors(),
	}
}

// Get returns a Todo entity by its id.
func (c *TodoClient) Get(ctx context.Context, id uuid.UUID) (*Todo, error) {
	return c.Query().Where(todo.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *TodoClient) GetX(ctx context.Context, id uuid.UUID) *Todo {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *TodoClient) Hooks() []Hook {
	return c.hooks.Todo
}

// Interceptors returns the client interceptors.
func (c *TodoClient) Interceptors() []Interceptor {
	return c.inters.Todo
}

func (c *TodoClient) mutate(ctx context.Context, m *TodoMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&TodoCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&TodoUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&TodoUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&TodoDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown Todo mutation op: %q", m.Op())
	}
}

// TodoHistoryClient is a client for the TodoHistory schema.
type TodoHistoryClient struct {
	config
}

// NewTodoHistoryClient returns a client for the TodoHistory from the given config.
func NewTodoHistoryClient(c config) *TodoHistoryClient {
	return &TodoHistoryClient{config: c}
}

// Use adds a list of mutation hooks to the hooks stack.
// A call to `Use(f, g, h)` equals to `todohistory.Hooks(f(g(h())))`.
func (c *TodoHistoryClient) Use(hooks ...Hook) {
	c.hooks.TodoHistory = append(c.hooks.TodoHistory, hooks...)
}

// Intercept adds a list of query interceptors to the interceptors stack.
// A call to `Intercept(f, g, h)` equals to `todohistory.Intercept(f(g(h())))`.
func (c *TodoHistoryClient) Intercept(interceptors ...Interceptor) {
	c.inters.TodoHistory = append(c.inters.TodoHistory, interceptors...)
}

// Create returns a builder for creating a TodoHistory entity.
func (c *TodoHistoryClient) Create() *TodoHistoryCreate {
	mutation := newTodoHistoryMutation(c.config, OpCreate)
	return &TodoHistoryCreate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// CreateBulk returns a builder for creating a bulk of TodoHistory entities.
func (c *TodoHistoryClient) CreateBulk(builders ...*TodoHistoryCreate) *TodoHistoryCreateBulk {
	return &TodoHistoryCreateBulk{config: c.config, builders: builders}
}

// MapCreateBulk creates a bulk creation builder from the given slice. For each item in the slice, the function creates
// a builder and applies setFunc on it.
func (c *TodoHistoryClient) MapCreateBulk(slice any, setFunc func(*TodoHistoryCreate, int)) *TodoHistoryCreateBulk {
	rv := reflect.ValueOf(slice)
	if rv.Kind() != reflect.Slice {
		return &TodoHistoryCreateBulk{err: fmt.Errorf("calling to TodoHistoryClient.MapCreateBulk with wrong type %T, need slice", slice)}
	}
	builders := make([]*TodoHistoryCreate, rv.Len())
	for i := 0; i < rv.Len(); i++ {
		builders[i] = c.Create()
		setFunc(builders[i], i)
	}
	return &TodoHistoryCreateBulk{config: c.config, builders: builders}
}

// Update returns an update builder for TodoHistory.
func (c *TodoHistoryClient) Update() *TodoHistoryUpdate {
	mutation := newTodoHistoryMutation(c.config, OpUpdate)
	return &TodoHistoryUpdate{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOne returns an update builder for the given entity.
func (c *TodoHistoryClient) UpdateOne(th *TodoHistory) *TodoHistoryUpdateOne {
	mutation := newTodoHistoryMutation(c.config, OpUpdateOne, withTodoHistory(th))
	return &TodoHistoryUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// UpdateOneID returns an update builder for the given id.
func (c *TodoHistoryClient) UpdateOneID(id uuid.UUID) *TodoHistoryUpdateOne {
	mutation := newTodoHistoryMutation(c.config, OpUpdateOne, withTodoHistoryID(id))
	return &TodoHistoryUpdateOne{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// Delete returns a delete builder for TodoHistory.
func (c *TodoHistoryClient) Delete() *TodoHistoryDelete {
	mutation := newTodoHistoryMutation(c.config, OpDelete)
	return &TodoHistoryDelete{config: c.config, hooks: c.Hooks(), mutation: mutation}
}

// DeleteOne returns a builder for deleting the given entity.
func (c *TodoHistoryClient) DeleteOne(th *TodoHistory) *TodoHistoryDeleteOne {
	return c.DeleteOneID(th.ID)
}

// DeleteOneID returns a builder for deleting the given entity by its id.
func (c *TodoHistoryClient) DeleteOneID(id uuid.UUID) *TodoHistoryDeleteOne {
	builder := c.Delete().Where(todohistory.ID(id))
	builder.mutation.id = &id
	builder.mutation.op = OpDeleteOne
	return &TodoHistoryDeleteOne{builder}
}

// Query returns a query builder for TodoHistory.
func (c *TodoHistoryClient) Query() *TodoHistoryQuery {
	return &TodoHistoryQuery{
		config: c.config,
		ctx:    &QueryContext{Type: TypeTodoHistory},
		inters: c.Interceptors(),
	}
}

// Get returns a TodoHistory entity by its id.
func (c *TodoHistoryClient) Get(ctx context.Context, id uuid.UUID) (*TodoHistory, error) {
	return c.Query().Where(todohistory.ID(id)).Only(ctx)
}

// GetX is like Get, but panics if an error occurs.
func (c *TodoHistoryClient) GetX(ctx context.Context, id uuid.UUID) *TodoHistory {
	obj, err := c.Get(ctx, id)
	if err != nil {
		panic(err)
	}
	return obj
}

// Hooks returns the client hooks.
func (c *TodoHistoryClient) Hooks() []Hook {
	return c.hooks.TodoHistory
}

// Interceptors returns the client interceptors.
func (c *TodoHistoryClient) Interceptors() []Interceptor {
	return c.inters.TodoHistory
}

func (c *TodoHistoryClient) mutate(ctx context.Context, m *TodoHistoryMutation) (Value, error) {
	switch m.Op() {
	case OpCreate:
		return (&TodoHistoryCreate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdate:
		return (&TodoHistoryUpdate{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpUpdateOne:
		return (&TodoHistoryUpdateOne{config: c.config, hooks: c.Hooks(), mutation: m}).Save(ctx)
	case OpDelete, OpDeleteOne:
		return (&TodoHistoryDelete{config: c.config, hooks: c.Hooks(), mutation: m}).Exec(ctx)
	default:
		return nil, fmt.Errorf("ent: unknown TodoHistory mutation op: %q", m.Op())
	}
}

// hooks and interceptors per client, for fast access.
type (
	hooks struct {
		TestExclude, TestSkip, TestSkipHistory, Todo, TodoHistory []ent.Hook
	}
	inters struct {
		TestExclude, TestSkip, TestSkipHistory, Todo, TodoHistory []ent.Interceptor
	}
)
